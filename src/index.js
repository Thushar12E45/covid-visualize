/* const moment = require('moment');
const covidData = require('../public/covidData');

function start() {
  const filteredCovidData = {};
  const prevDay = {};
  for (const singleDay of covidData) {
    const date = moment(singleDay.Date).format('l');
    const prevDate = moment(date).subtract(1, 'days').format('l');
    if(filteredCovidData.date){

    }
    else{
      filteredCovidData.date={};

    }
  }
}
start(); */
